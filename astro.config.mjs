import { defineConfig } from 'astro/config';

export default defineConfig({
  // GitHub Pages user site — replace with your own https://<username>.github.io
  site: 'https://tomcarpen.github.io',
});
